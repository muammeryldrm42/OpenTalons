# Security Policy

## Reporting a Vulnerability
Use GitHub Security Advisories if available.

## Security Model (high level)
- Default-deny permissions for skills
- Policy-gated tool calls (timeouts, size limits, allowlists)
- No arbitrary exec by default
